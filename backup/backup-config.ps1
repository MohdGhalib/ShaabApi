# ═══════════════════════════════════════════════════════════════════════════
#   إعداد النسخ الاحتياطي  —  الملف الوحيد الذي يحتاج المبرمج تعديله
#   (يُقرأ تلقائياً من shaab-backup.ps1 و shaab-restore.ps1)
# ═══════════════════════════════════════════════════════════════════════════

# ── 1) اتصال قاعدة البيانات MySQL على السيرفر الداخلي ──
$DbHost     = 'localhost'                 # عنوان خادم MySQL (غالباً localhost على نفس السيرفر)
$DbPort     = '3306'
$DbName     = 'shaab_db'                  # اسم قاعدة البيانات الفعلي
$DbUser     = 'root'                      # مستخدم له صلاحية القراءة الكاملة + LOCK
$DbPassword = '<<< ضع كلمة مرور القاعدة هنا >>>'

# ── 2) مسار أدوات MySQL (المجلد bin الذي يحوي mysqldump.exe و mysql.exe) ──
#     اتركه '' لو كانت الأدوات ضمن PATH، أو ضع المسار الكامل:
$MysqlBinDir = ''   # مثال: 'C:\Program Files\MySQL\MySQL Server 8.0\bin'

# ── 3) مجلدات النسخ ──
$BackupDir     = 'C:\ShaabBackups'        # المجلد الأساسي على السيرفر (الطبقة 1)
$OffsiteDir    = ''                       # خارج الموقع: قرص خارجي/NAS/مزامنة سحابية — '' = معطّل (الطبقة 2)
$RetentionDays = 30                       # كم يوماً نحتفظ بالنسخ (الأقدم يُحذف تلقائياً)

# ملاحظة: لو ضُبطت متغيّرات بيئة التطبيق (MYSQL_HOST/MYSQLDATABASE/MYSQLUSER/MYSQLPASSWORD)
# فستُستخدم تلقائياً متى تركتَ القيم أعلاه على الافتراضي — لكن يُفضّل ضبطها هنا صراحةً.
