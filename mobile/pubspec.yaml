name: shaab_app
description: تطبيق الشعب - نظام المنتسيات
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # شبكة واستيثاق
  http: ^1.2.0
  shared_preferences: ^2.2.2
  local_auth: ^2.2.0

  # كاميرا وصور
  image_picker: ^1.0.7
  flutter_image_compress: ^2.1.0

  # واجهة
  google_fonts: ^6.2.1

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
